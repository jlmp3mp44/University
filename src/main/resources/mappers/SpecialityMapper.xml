<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.solvd.university.dao.PersonRepository">

    <insert id="createSpeciality"  keyColumn="id" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO specialities(title, description, cafedra_id) VALUES (#{title}, #{description}, #{cafedraId})
    </insert>

    <update id="updateSpeciality">
        UPDATE specialities SET title = #{title} WHERE id = #{id}
    </update>
</mapper>